## AWS Backend

This backend is deployed via AWS Console.

Lambdas:
- GET /racers → get_racers.py
- POST /racers → add_racer.py
- PUT /racers → update_results.py

API Gateway routes are configured manually.
DynamoDB table: RegistrationResults
